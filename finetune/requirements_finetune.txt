# Fine-tuning dependencies — install on vast.ai GPU instance
# 1. Install Unsloth first (must be before transformers/trl):
#    pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# 2. Then install the rest:
#    pip install -r requirements_finetune.txt

torch>=2.1.0
transformers>=4.46.0
trl>=0.12.0
peft>=0.13.0
datasets>=3.0.0
bitsandbytes>=0.44.0
accelerate>=0.34.0
sentencepiece
huggingface_hub

# Optional — for experiment tracking
# wandb
